# Titanic_LogisticRegression
Titanic survival prediction using logistic regression. Covers the full ML workflow: data cleaning, feature engineering (FamilySize, IsAlone, Title), one-hot encoding, feature scaling, model training, and evaluation. Achieves ~81% accuracy on the test set.
